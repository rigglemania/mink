// Workspace settings (overrides default and user settings)
{
    "files.exclude": {
        ".git/**": true,
        "dist": true,
        "node_modules/**": true,
        "app/node_modules/**": true,
        "app/**/*.js": true,
        "app/**/*.js.map": true
    },

    // use the typescript version specified in package.json
    "typescript.tsdk": "node_modules/typescript/lib"
}